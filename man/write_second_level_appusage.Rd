% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/second_level.R
\name{write_second_level_appusage}
\alias{write_second_level_appusage}
\title{Write a second-level APP Usage cache}
\usage{
write_second_level_appusage(
  first_level_rda,
  output_dir = NULL,
  overwrite = FALSE,
  include_collection_app = TRUE,
  max_episode_ms = 24 * 60 * 60 * 1000,
  max_daily_app_ms = 24 * 60 * 60 * 1000,
  reconstruct_meta = TRUE,
  meta_pairing = c("package", "package_class"),
  meta_start_event_types = 1,
  meta_end_event_types = c(2, 23),
  merge_meta_episodes = TRUE,
  meta_episode_merge_gap_ms = 30 * 1000,
  meta_daily_source = c("summary", "episodes", "both"),
  tz = "Asia/Shanghai",
  inline_qc = TRUE,
  require_all_weekdays = TRUE,
  min_nonempty_days = 7,
  use_all_apps_row = FALSE,
  drop_likely_total_all_rows = TRUE,
  all_row_tolerance = 0.1,
  max_daily_total_ms = 24 * 60 * 60 * 1000,
  max_export_lookback_days = 31,
  meta_diff_abs_ms = 60 * 1000,
  meta_diff_ratio = 0.2
)
}
\arguments{
\item{first_level_rda}{Path to a first-level RDA file.}

\item{output_dir}{Output directory. Defaults to the sibling \code{proclevel-2}
directory when \code{first_level_rda} is under \code{proclevel-1}, otherwise the
first-level RDA directory.}

\item{overwrite}{Whether to overwrite an existing output file.}

\item{include_collection_app}{Whether to keep \code{com.w.appusage} rows in
second-level outputs.}

\item{max_episode_ms}{Episode duration above this value is flagged as an
anomaly.}

\item{max_daily_app_ms}{Daily app duration above this value is flagged as an
anomaly.}

\item{reconstruct_meta}{Whether to explicitly reconstruct meta Table 2 events
into episode records. Defaults to \code{TRUE} for second-level meta output.}

\item{meta_pairing}{Pairing strategy for meta event reconstruction.}

\item{meta_start_event_types}{Event types that start meta episodes.}

\item{meta_end_event_types}{Event types that end meta episodes.}

\item{merge_meta_episodes}{Whether to merge adjacent reconstructed meta
episodes for the same app when the time gap is small.}

\item{meta_episode_merge_gap_ms}{Maximum gap, in milliseconds, allowed when
merging adjacent reconstructed meta episodes.}

\item{meta_daily_source}{Source for meta daily rows.}

\item{tz}{Effective IANA time zone used for timestamp-derived dates and daily
interval boundaries.}

\item{inline_qc}{Whether to compute routine daily QC metadata while the
second-level object is still in memory.}

\item{require_all_weekdays}{Whether inline daily QC requires Monday through
Sunday coverage.}

\item{min_nonempty_days}{Minimum number of non-empty recorded days required
by inline daily QC.}

\item{use_all_apps_row, drop_likely_total_all_rows, all_row_tolerance}{Routine
daily QC controls passed to \code{\link[=qc_appusage_day]{qc_appusage_day()}}.}

\item{max_daily_total_ms, max_export_lookback_days, meta_diff_abs_ms, meta_diff_ratio}{Anomaly/QC thresholds used for inline metadata.}
}
\value{
Invisibly returns the written second-level RDA path.
}
\description{
Reads a first-level APP Usage RDA file containing exactly one object named
\code{data}, converts it with \code{make_second_level_appusage()}, and writes matching
\code{proc-2} RDA and JSON files. The RDA still contains exactly one object named
\code{data}; the JSON stores second-level transformation metadata.
}
