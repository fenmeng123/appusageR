% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/second_level.R
\name{make_second_level_appusage}
\alias{make_second_level_appusage}
\title{Create second-level APP Usage analysis data}
\usage{
make_second_level_appusage(
  data,
  export_type = NULL,
  include_collection_app = TRUE,
  max_episode_ms = 24 * 60 * 60 * 1000,
  max_daily_app_ms = 24 * 60 * 60 * 1000,
  reconstruct_meta = TRUE,
  meta_pairing = c("package", "package_class"),
  meta_start_event_types = 1,
  meta_end_event_types = c(2, 23),
  merge_meta_episodes = TRUE,
  meta_episode_merge_gap_ms = 30 * 1000,
  meta_daily_source = c("summary", "episodes", "both"),
  tz = "Asia/Shanghai"
)
}
\arguments{
\item{data}{First-level APP Usage data. This may be a first-level RDA \code{data}
list, a parser tibble, or the list returned by \code{parse_meta()}.}

\item{export_type}{Optional export type override: \code{"line"}, \code{"meta"},
\code{"day"}, or \code{"app"}.}

\item{include_collection_app}{Whether to keep \code{com.w.appusage} rows in
second-level outputs.}

\item{max_episode_ms}{Episode duration above this value is flagged as an
anomaly.}

\item{max_daily_app_ms}{Daily app duration above this value is flagged as an
anomaly.}

\item{reconstruct_meta}{Whether to explicitly reconstruct meta Table 2 events
into episode records. Defaults to \code{TRUE} for research-ready second-level
meta output; \code{parse_meta()} itself remains a faithful raw parser.}

\item{meta_pairing}{Pairing strategy for meta event reconstruction.}

\item{meta_start_event_types}{Event types that start meta episodes.}

\item{meta_end_event_types}{Event types that end meta episodes.}

\item{merge_meta_episodes}{Whether to merge adjacent reconstructed meta
episodes for the same app when the time gap is small.}

\item{meta_episode_merge_gap_ms}{Maximum gap, in milliseconds, allowed when
merging adjacent reconstructed meta episodes.}

\item{meta_daily_source}{Source for meta daily rows. \code{"summary"} keeps Table
1 summary-derived daily rows, \code{"episodes"} uses explicitly reconstructed
Table 2 episodes, and \code{"both"} returns both with provenance.}

\item{tz}{Effective IANA time zone. Defaults to \code{"Asia/Shanghai"} and is used
for every timestamp-derived date and daily boundary.}
}
\value{
A list with \code{event}, \code{episode}, and \code{daily} tibbles.
}
\description{
Converts first-level APP Usage parser output into a stable second-level list
with three analysis grains: \code{event}, \code{episode}, and \code{daily}. Unavailable
grains are returned as zero-row tibbles. Meta Table 2 is preserved as event
data. Meta event-to-episode reconstruction is explicit, diagnosable, and
enabled by default in second-level meta processing.
}
