% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{read_appusage_batch}
\alias{read_appusage_batch}
\title{Batch preprocess APP Usage exports}
\usage{
read_appusage_batch(
  x,
  ids = NULL,
  self_report = NULL,
  participant_id_col = NULL,
  wenjuanxing_sequence_col = NULL,
  type = "auto",
  input = "file",
  output_dir = NULL,
  project_name = NULL,
  project_id = NULL,
  tz = "Asia/Shanghai",
  encoding = "auto",
  strict = FALSE,
  overwrite = FALSE,
  progress = TRUE,
  progress_every = 100,
  parallel = FALSE,
  n_cores = 1,
  resume = FALSE,
  checkpoint_every = NULL,
  max_workers = 12,
  worker_cap_override = FALSE,
  retry_memory_allocation = TRUE,
  memory_retry_workers = 1,
  provenance = NULL
)
}
\arguments{
\item{x}{Character vector of file paths, raw text records, or line vectors.}

\item{ids}{Optional participant IDs parallel to \code{x}.}

\item{self_report}{Optional self-report data frame. Reserved for future
Wenjuanxing sequence-to-study-ID matching; currently errors if supplied.}

\item{participant_id_col}{Reserved study participant ID column name.}

\item{wenjuanxing_sequence_col}{Reserved Wenjuanxing sequence column name.}

\item{type}{Export type or \code{"auto"}.}

\item{input}{One of \code{"file"}, \code{"text"}, or \code{"lines"}.}

\item{output_dir}{Optional parent directory for the project output folder.}

\item{project_name}{Optional project name. Defaults to the next available
\code{StudyN} name under \code{output_dir}.}

\item{project_id}{Optional short project ID. Defaults to a generated
four-character hexadecimal ID.}

\item{tz}{Time zone passed to parsers.}

\item{encoding}{Source encoding.}

\item{strict}{If \code{TRUE}, stop on the first error after recording diagnostics.}

\item{overwrite}{Whether to overwrite existing cache files.}

\item{progress}{If \code{TRUE}, print simple progress messages every
\code{progress_every} files.}

\item{progress_every}{Progress interval.}

\item{parallel}{Whether to use parallel workers. Defaults to \code{FALSE}.}

\item{n_cores}{Requested workers when \code{parallel = TRUE}; effective workers
are capped by available cores and first-level memory-risk heuristics.}

\item{resume}{Whether to reuse durable first-level checkpoint rows when
resuming an interrupted first-level batch.}

\item{checkpoint_every}{Write a durable checkpoint summary after this many
processed files. Defaults to \code{progress_every}.}

\item{max_workers}{Maximum ordinary first-level parallel workers.}

\item{worker_cap_override}{Whether to bypass the ordinary first-level worker
cap while still respecting available cores and file count.}

\item{retry_memory_allocation}{Whether to retry memory-allocation failures
with a reduced worker count.}

\item{memory_retry_workers}{Worker count recorded for memory retries.}

\item{provenance}{Optional implementation provenance supplied by a project
workflow. Standalone calls compute one provenance record for the batch.}
}
\value{
Invisibly returns a tibble summary. It does not return parsed data.
}
\description{
Preprocesses many APP Usage \code{.txt} exports without returning parsed data into
memory. When \code{output_dir} is supplied, the function creates a BIDS-like
project folder and creates \code{proclevel-1} only because first-level
preprocessing is the only step executed by this function. Later processing
functions create their own processing-level folders when they run.
}
