% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rebuild_plan.R
\name{plan_appusage_project_rebuild}
\alias{plan_appusage_project_rebuild}
\title{Preview targeted APP Usage cache rebuild actions}
\usage{
plan_appusage_project_rebuild(
  project_dir,
  current_provenance = NULL,
  tz = appusage_default_timezone(),
  write_plan = FALSE,
  plan_file = NULL
)
}
\arguments{
\item{project_dir}{Existing appusageR project output directory.}

\item{current_provenance}{Optional current implementation provenance. When
omitted, it is computed from the loaded package with \code{tz}.}

\item{tz}{Effective timezone for provenance comparison. Defaults to
\code{"Asia/Shanghai"}.}

\item{write_plan}{Whether to explicitly write the preview CSV. Defaults to
\code{FALSE}; planning itself is non-mutating.}

\item{plan_file}{Optional preview CSV path. Defaults to
\code{rebuild_plan_preview.csv} under \code{project_dir} when writing is requested.}
}
\value{
A tibble with one row per manifest source, deterministic action and
reason fields, cache-pair state and provenance comparisons.
}
\description{
Builds a metadata-only, one-row-per-source rebuild plan from an existing
project output. It reads manifests, CSV summaries and JSON metadata, never
loads RDA payloads, and never launches preprocessing. Selected second-level
rows can subsequently be passed to \code{\link[=rerun_second_level_project_subset]{rerun_second_level_project_subset()}} by
\code{source_record_key}.
}
