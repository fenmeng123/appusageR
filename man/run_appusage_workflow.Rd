% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_wrappers.R
\name{run_appusage_workflow}
\alias{run_appusage_workflow}
\title{Run the standard APP Usage preprocessing workflow}
\usage{
run_appusage_workflow(
  x,
  output_dir,
  ids = NULL,
  type = "auto",
  input = "file",
  tz = "Asia/Shanghai",
  encoding = "auto",
  strict = FALSE,
  run_second_level = TRUE,
  run_qc = TRUE,
  dictionary = NULL,
  run_category = !is.null(dictionary),
  progress = TRUE,
  parallel = FALSE,
  n_cores = 1,
  overwrite = FALSE,
  project_name = NULL,
  project_id = NULL,
  reconstruct_meta = TRUE,
  meta_pairing = c("package", "package_class"),
  meta_start_event_types = 1,
  meta_end_event_types = c(2, 23),
  merge_meta_episodes = TRUE,
  meta_episode_merge_gap_ms = 30 * 1000,
  meta_daily_source = c("summary", "episodes", "both")
)
}
\arguments{
\item{x}{Input vector passed to \code{\link[=read_appusage_batch]{read_appusage_batch()}}.}

\item{output_dir}{Parent output directory for the BIDS-like project folder.}

\item{ids}{Optional participant IDs aligned with \code{x}.}

\item{type, input, tz, encoding, strict}{Existing first-level batch parameters.}

\item{run_second_level}{Whether to run second-level preprocessing.}

\item{run_qc}{Whether to update QC metadata.}

\item{dictionary}{Optional category dictionary data frame or path.}

\item{run_category}{Whether to run category enrichment.}

\item{progress, parallel, n_cores}{Existing batch workflow controls.}

\item{overwrite}{Whether cache outputs may be overwritten.}

\item{project_name, project_id}{Optional project folder identifiers.}

\item{reconstruct_meta}{Whether to explicitly reconstruct meta Table 2 events
during second-level preprocessing. Defaults to \code{TRUE}.}

\item{meta_pairing}{Pairing strategy for meta event reconstruction.}

\item{meta_start_event_types}{Event types that start meta episodes.}

\item{meta_end_event_types}{Event types that end meta episodes.}

\item{merge_meta_episodes}{Whether to merge adjacent complete reconstructed
meta episodes for the same app when their time gap is small.}

\item{meta_episode_merge_gap_ms}{Maximum non-negative gap, in milliseconds,
allowed when merging adjacent reconstructed meta episodes.}

\item{meta_daily_source}{Source for meta daily rows.}
}
\value{
Invisibly returns an \code{appusage_workflow_result} list of compact
summaries and paths.
}
\description{
Module 6 high-level wrapper around existing batch preprocessing, second-level
transformation, routine QC, and optional category enrichment.
}
