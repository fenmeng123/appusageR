% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/second_level.R
\name{reconstruct_meta_episodes}
\alias{reconstruct_meta_episodes}
\title{Reconstruct meta event episodes}
\usage{
reconstruct_meta_episodes(
  events,
  pairing = c("package", "package_class"),
  start_event_types = 1,
  end_event_types = c(2, 23),
  tz = "Asia/Shanghai",
  max_episode_ms = 24 * 60 * 60 * 1000,
  merge_contiguous = TRUE,
  merge_gap_ms = 30 * 1000,
  ...
)
}
\arguments{
\item{events}{Meta event data frame, usually \code{parse_meta(...)$events} or a
first-level \code{meta_events} table.}

\item{pairing}{Pair within \code{package_name} or within \code{package_name} plus
\code{class_name}.}

\item{start_event_types}{Event types treated as starts.}

\item{end_event_types}{Event types treated as ends.}

\item{tz}{Time zone used when timestamps must be converted to datetimes.}

\item{max_episode_ms}{Episode duration above this value is flagged as an
anomaly.}

\item{merge_contiguous}{Whether to merge adjacent complete reconstructed
episodes for the same app when their time gap is small.}

\item{merge_gap_ms}{Maximum non-negative gap, in milliseconds, allowed when
merging adjacent reconstructed meta episodes.}

\item{...}{Reserved for future reconstruction options.}
}
\value{
A second-level episode tibble compatible with line-derived episodes.
The tibble carries a \code{meta_reconstruction_diagnostics} attribute for JSON
metadata writers.
}
\description{
Explicitly converts APP Usage meta Table 2 event logs into episode-level
records. This function is not called by \code{\link[=parse_meta]{parse_meta()}}. It is called by
default in second-level meta workflows, with unmatched-event diagnostics
stored in metadata.
}
