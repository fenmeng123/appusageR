% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_wrappers.R
\name{run_second_level_appusage}
\alias{run_second_level_appusage}
\title{Run second-level APP Usage preprocessing for one source}
\usage{
run_second_level_appusage(
  x,
  export_type = NULL,
  output_dir = NULL,
  write_cache = !is.null(output_dir),
  overwrite = FALSE,
  include_collection_app = TRUE,
  max_episode_ms = 24 * 60 * 60 * 1000,
  max_daily_app_ms = 24 * 60 * 60 * 1000,
  reconstruct_meta = TRUE,
  meta_pairing = c("package", "package_class"),
  meta_start_event_types = 1,
  meta_end_event_types = c(2, 23),
  merge_meta_episodes = TRUE,
  meta_episode_merge_gap_ms = 30 * 1000,
  meta_daily_source = c("summary", "episodes", "both")
)
}
\arguments{
\item{x}{First-level wrapper result, first-level RDA path, parser output, or
first-level data list.}

\item{export_type}{Optional export type override for in-memory data.}

\item{output_dir}{Optional directory for \code{proc-2} cache files.}

\item{write_cache}{Whether to write \code{proc-2} cache files.}

\item{overwrite}{Whether existing cache files may be overwritten.}

\item{include_collection_app}{Whether to keep \code{com.w.appusage} rows.}

\item{max_episode_ms}{Episode anomaly threshold.}

\item{max_daily_app_ms}{Daily-app anomaly threshold.}

\item{reconstruct_meta}{Whether to explicitly reconstruct meta Table 2 events
into episode records. Defaults to \code{TRUE} for second-level meta output.}

\item{meta_pairing}{Pairing strategy for meta event reconstruction.}

\item{meta_start_event_types}{Event types that start meta episodes.}

\item{meta_end_event_types}{Event types that end meta episodes.}

\item{merge_meta_episodes}{Whether to merge adjacent complete reconstructed
meta episodes for the same app when their time gap is small.}

\item{meta_episode_merge_gap_ms}{Maximum non-negative gap, in milliseconds,
allowed when merging adjacent reconstructed meta episodes.}

\item{meta_daily_source}{Source for meta daily rows.}
}
\value{
An \code{appusage_second_level} list.
}
\description{
Module 3 wrapper around \code{\link[=make_second_level_appusage]{make_second_level_appusage()}} and
\code{\link[=write_second_level_appusage]{write_second_level_appusage()}}. Meta episode reconstruction is explicit and
enabled by default in second-level meta processing.
}
