% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_workflow.R
\name{run_appusage_project_workflow}
\alias{run_appusage_project_workflow}
\title{Run a project-level APP Usage workflow}
\usage{
run_appusage_project_workflow(
  project_dir = NULL,
  output_root,
  raw_data_root = NULL,
  project_id = NULL,
  project_name = NULL,
  self_report_file = NULL,
  sequence_col = "序号",
  upload_col = NULL,
  submit_time_col = NULL,
  max_files = Inf,
  self_report_n_max = Inf,
  self_report_sheet = 1,
  self_report_guess_max = NULL,
  self_report_col_types = NULL,
  resume = TRUE,
  export_type_priority = c("line", "meta", "day", "app"),
  dry_run = FALSE,
  type = "auto",
  tz = "Asia/Shanghai",
  encoding = "auto",
  strict = FALSE,
  overwrite = FALSE,
  progress = TRUE,
  parallel = FALSE,
  n_cores = 1,
  first_level_max_workers = 12,
  first_level_worker_cap_override = FALSE,
  first_level_checkpoint_every = NULL,
  retry_memory_allocation = TRUE,
  memory_retry_workers = 1,
  run_second_level = TRUE,
  run_qc = TRUE,
  diagnostic_verbosity = c("summary", "full", "none"),
  ...
)
}
\arguments{
\item{project_dir}{Optional direct project directory. If supplied, this
preserves the 0.2.10 direct-folder behavior.}

\item{output_root}{Parent output directory.}

\item{raw_data_root}{Optional root containing multiple project folders and
Wenjuanxing Excel files.}

\item{project_id}{Optional ProjectID. Required when \code{raw_data_root} is used.}

\item{project_name}{Optional project/study label for validation and output
naming.}

\item{self_report_file}{Optional paired Wenjuanxing/self-report file path.}

\item{sequence_col}{Wenjuanxing sequence column. Defaults to
\code{"\\\\u5e8f\\\\u53f7"}.}

\item{upload_col}{Project-specific upload/file column for self-report
matching.}

\item{submit_time_col}{Optional Wenjuanxing submit-time column for duplicate
candidate tie-breaking.}

\item{max_files}{Maximum number of \code{.txt} files to preprocess, selected by
lexicographic basename order. Intended for bounded smoke tests.}

\item{self_report_n_max}{Maximum number of self-report rows to read.}

\item{self_report_sheet}{Excel sheet name or index to read from the paired
self-report workbook. Defaults to the first sheet.}

\item{self_report_guess_max}{Number of selected rows used by \code{\link[readxl:read_excel]{readxl::read_excel()}}
for type inference. The default uses the full selected row range: \code{Inf}
when \code{self_report_n_max} is unlimited, otherwise \code{self_report_n_max}.}

\item{self_report_col_types}{Optional readxl-compatible column types. When
supplied, read/coercion warnings are retained in project diagnostics.}

\item{resume}{Whether to reuse compatible existing outputs.}

\item{export_type_priority}{Export-type priority for duplicate upload
candidates.}

\item{dry_run}{If \code{TRUE}, write only diagnostics/project manifest and
workflow configuration output.}

\item{type, tz, encoding}{Existing parser controls passed to
\code{\link[=read_appusage_batch]{read_appusage_batch()}}.}

\item{strict}{Whether to stop after writing diagnostic reports for failures.}

\item{overwrite}{Whether to overwrite the selected output study folder.}

\item{progress}{Whether delegated batch functions should report progress.}

\item{parallel, n_cores}{Parallel controls. Defaults are serial.}

\item{first_level_max_workers}{Maximum ordinary first-level workers before
adaptive memory-risk caps are applied.}

\item{first_level_worker_cap_override}{Whether to bypass adaptive
first-level memory-risk caps after explicitly accepting the risk.}

\item{first_level_checkpoint_every}{Checkpoint interval for first-level
resume summaries. Defaults to the first-level batch default.}

\item{retry_memory_allocation}{Whether first-level memory-allocation failures
should be retried with reduced workers.}

\item{memory_retry_workers}{Worker count recorded for memory-allocation
retries.}

\item{run_second_level}{Whether to run second-level processing.}

\item{run_qc}{Whether to run QC metadata after second-level processing.}

\item{diagnostic_verbosity}{Console diagnostic behavior for failures:
\code{"summary"} prints source context and report paths, \code{"full"} prints the
full issue report, and \code{"none"} suppresses diagnostic console output.}

\item{...}{Additional arguments passed to \code{\link[=write_second_level_batch]{write_second_level_batch()}}.}
}
\value{
An \code{appusage_project_workflow} list with compact summaries and paths.
}
\description{
Builds a project manifest, optionally performs a dry run, or delegates to the
existing first-level, second-level, and QC batch functions with serial
processing by default. Starting in 0.3.0, this wrapper can also discover a
project folder and paired Wenjuanxing Excel file from a raw-data root and
write a matched self-report Excel file.
}
