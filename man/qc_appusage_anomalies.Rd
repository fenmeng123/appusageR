% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anomaly_qc.R
\name{qc_appusage_anomalies}
\alias{qc_appusage_anomalies}
\title{Summarize anomaly and reasonableness checks for appusage data}
\usage{
qc_appusage_anomalies(
  data,
  metadata = NULL,
  max_episode_ms = 24 * 60 * 60 * 1000,
  max_daily_app_ms = 24 * 60 * 60 * 1000,
  max_daily_total_ms = 24 * 60 * 60 * 1000,
  max_export_lookback_days = 31,
  meta_diff_abs_ms = 60 * 1000,
  meta_diff_ratio = 0.2,
  source_qc_config = NULL
)
}
\arguments{
\item{data}{A second-level appusage list with \code{event}, \code{episode}, and/or
\code{daily} data frames, or a single data frame.}

\item{metadata}{Optional metadata list used for export-date-span checks.}

\item{max_episode_ms}{Maximum plausible episode duration in milliseconds.}

\item{max_daily_app_ms}{Maximum plausible app-level daily duration in
milliseconds.}

\item{max_daily_total_ms}{Maximum plausible summed daily duration in
milliseconds within one source file.}

\item{max_export_lookback_days}{Maximum expected lookback in days from native
export timestamp to observed record dates.}

\item{meta_diff_abs_ms}{Absolute meta summary-vs-episode duration difference
threshold in milliseconds.}

\item{meta_diff_ratio}{Relative meta summary-vs-episode duration difference
threshold.}

\item{source_qc_config}{Optional named list overriding the coherent 0.3.4-F
source-anomaly QC thresholds. This changes flags and eligibility only;
source rows are never removed or rewritten.}
}
\value{
A compact list of anomaly metrics suitable for JSON metadata.
}
\description{
\code{qc_appusage_anomalies()} counts implausible records in second-level appusage
data without filtering or modifying the data. The checks are intended for
metadata and summary reporting.
}
