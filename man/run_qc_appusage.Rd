% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_wrappers.R
\name{run_qc_appusage}
\alias{run_qc_appusage}
\title{Run APP Usage QC}
\usage{
run_qc_appusage(
  x = NULL,
  project_dir = NULL,
  participant_col = "participant_id",
  strict = FALSE,
  overwrite = TRUE,
  progress = TRUE,
  require_all_weekdays = TRUE,
  min_nonempty_days = 7,
  use_all_apps_row = FALSE,
  include_collection_app = TRUE,
  drop_likely_total_all_rows = TRUE,
  all_row_tolerance = 0.1,
  max_episode_ms = 24 * 60 * 60 * 1000,
  max_daily_app_ms = 24 * 60 * 60 * 1000,
  max_daily_total_ms = 24 * 60 * 60 * 1000,
  max_export_lookback_days = 31,
  meta_diff_abs_ms = 60 * 1000,
  meta_diff_ratio = 0.2
)
}
\arguments{
\item{x}{A daily data frame, second-level data list, or second-level wrapper
result.}

\item{project_dir}{Optional project directory for metadata-writing QC.}

\item{participant_col}{Participant column for in-memory daily QC.}

\item{strict, overwrite, progress}{Passed to \code{\link[=write_qc_metadata_batch]{write_qc_metadata_batch()}} in
project mode.}

\item{require_all_weekdays, min_nonempty_days, use_all_apps_row, include_collection_app, drop_likely_total_all_rows, all_row_tolerance}{Existing QC rule parameters.}

\item{max_episode_ms, max_daily_app_ms, max_daily_total_ms}{Numeric anomaly
thresholds in milliseconds for project-mode QC metadata.}

\item{max_export_lookback_days}{Maximum expected lookback in days from native
export timestamp to observed record dates.}

\item{meta_diff_abs_ms, meta_diff_ratio}{Thresholds for meta summary-vs-episode
duration disagreement checks.}
}
\value{
A QC tibble or project summary tibble.
}
\description{
Module 4 wrapper for in-memory daily QC or project-level QC metadata updates.
In project mode this calls \code{\link[=write_qc_metadata_batch]{write_qc_metadata_batch()}} and updates \code{proc-2}
JSON metadata without creating \code{proclevel-3}.
}
